VLAN L3VNI - 100 (L3VNI 30001)
VLAN L2VNI / Access - 1000 (L2VNI 300001)
VRF - VRF-A
Access subnet: 192.168.123.0/24

leaf-1, leaf-2, leaf-3 and leaf-4

vlan 100
  vn-segment 30001

vlan 1000
  vn-segment 300001

vrf context VRF-A
  vni 30001
  rd auto
  address-family ipv4 unicast
    route-target both auto
    route-target both auto evpn

interface Vlan100
  no shutdown
  no autostate
  vrf member VRF-A
  no ip redirects
  ip forward
  no ipv6 redirects

interface Vlan1000
  no shutdown
  no autostate
  vrf member VRF-A
  no ip redirects
  ip address 192.168.123.1/24 tag 21921
  no ipv6 redirects
  fabric forwarding mode anycast-gateway

interface nve1
  member vni 30001 associate-vrf
  member vni 300001
    mcast-group 233.0.0.1

evpn
  vni 300001 l2
    rd auto
    route-target import auto
    route-target export auto

route-map TAG permit 10
  match tag 21921 

router bgp 65500
  vrf VRF-A
    address-family ipv4 unicast
      advertise l2vpn evpn
      redistribute direct route-map TAG


leaf-1 / leaf-2

interface port-channel1000
  switchport access vlan 1000
  vpc 1000
  no shutdown

interface Ethernet1/2
  switchport
  switchport mode access
  switchport access vlan 1000
  channel-group 1000 mode active
  no shutdown

leaf-3 / leaf-4

interface port-channel1000
  switchport access vlan 1000
  vpc 1000
  no shutdown

interface Ethernet1/1
  switchport
  switchport mode access
  switchport access vlan 1000
  channel-group 1000 mode active
  no shutdown
